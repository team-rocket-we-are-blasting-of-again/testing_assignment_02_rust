Feature: Lowercase string


    Scenario: When a text string is entered, it must be output with all letters in lowercase
        Given a string of text
        When The text is <text>
        Then the output is <lowercaseText>

        Examples:
            | text         | reverseText    |
            | Hello World  | hello world    |
            | FOOBAR       | foobar         |